{% extends '::base.html.twig' %}

{% block body -%}
    <h1>{% trans %}Wall list{% endtrans %}</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Type</th>
                <th>Value</th>
                <th>Moderate</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('wall_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.type }}</td>
                <td>{{ entity.value }}</td>
                <td>{{ entity.moderate }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('wall_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('wall_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('wall_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
